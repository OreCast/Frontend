<section>
  <article>
      <h2>Site: {{.Site}}</h2>
      <hr/>
{{range $d := .Datasets}}
    {{$d.ETag}} &nbsp; | &nbsp; {{$d.LastModified}} &nbsp; | &nbsp;
    <a href="{{$d.Url}}">{{$d.Name}}</a>
    <br/>
{{end}}
  </article>
</section>
